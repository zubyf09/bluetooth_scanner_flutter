name: bluetooth
description: A new Flutter package.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.26.0
  equatable: ^1.2.3
  font_awesome_flutter: ^8.5.0
  flutter_blue: ^0.8.0

  values:
    path: ../../libraries/values
  core:
    path: ../../libraries/core
  widgets:
    path: ../../libraries/widgets
  network:
    path: ../../libraries/network
  navigation:
    path: ../../libraries/navigation

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
